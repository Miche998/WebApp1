"use strict";
let a = now.dayjs().isSame(now.dayjs());
console.log(a);
/**constdayjs = require("day")
function Task(id, description, urgent, pvt, deadline) {
    this.id=id;
    this.description = description;
    this.urgent = urgent;
    this.pvt = pvt;
    this.deadline = deadline;
}

function TaskList() {
    this.tasks = [];
    this.sortAndPrint() = () => ();
}

//function for sorting
function sortAndPrint(tasks) {
    for(task of)
}**/
